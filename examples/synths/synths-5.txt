The previous example setup a <code>keyMap</code> which defined the pitches we wanted to play in terms of their frequencies, in this example we're playing the exact same frequencies but this time using Tone.js's note strings.
